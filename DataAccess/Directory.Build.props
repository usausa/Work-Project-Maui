<Project>

  <PropertyGroup>
    <Deterministic>true</Deterministic>
    <LangVersion>latest</LangVersion>
    <NullableContextOptions>Enable</NullableContextOptions>
  </PropertyGroup>

  <PropertyGroup>
    <WarningsAsMessages>MSB3277</WarningsAsMessages>
  </PropertyGroup>

</Project>