<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="WorkMauiAot.MainPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml">

    <ScrollView>
        <VerticalStackLayout Padding="30,0" Spacing="25">
            <Image
                Aspect="AspectFit"
                HeightRequest="185"
                SemanticProperties.Description="dot net bot in a submarine number ten"
                Source="dotnet_bot.png" />

            <Label x:Name="FrameworkDescription" Style="{StaticResource Headline}" />
            <Label x:Name="RuntimeIdentifier" Style="{StaticResource Headline}" />
            <Label x:Name="Dynamic" Style="{StaticResource Headline}" />
            <Label x:Name="Build" Style="{StaticResource Headline}" />
        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
