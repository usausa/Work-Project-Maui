# UI 開発記録(完了履歴・決定事項・ナレッジ)

UI ブラッシュアップ関連の**記録の統合ファイル**(2026-07-07 統合。旧 `UIBrushUpPlan2_Log.md`+`_UIBrushUpPlan.md`+`_UI_Sample_Import_Plan(.Phase2).md`+`UI_Screen_Merge_Analysis.md` の要点を集約)。
**ドキュメントは本書と `UI_Verification_Checklist.md` の2本のみ**(2026-07-12 に `UI_Implementation_Plan.md` を Checklist へ統合し削除。残作業=画像アセットのコード反映も Checklist 末尾へ移動)。本書=完了履歴・決定事項・ナレッジ / Checklist=uibase→現在の画面別変更+実機確認+残作業。

## ステータス総括(2026-07-07 時点)

- **完了**: 第1弾ブラッシュアップ(全フェーズ・コミット済み)/ UISample 取り込み Ph1・Ph2 / アイコン共通化 / Graphics 名前空間分離 / Timeline→Graph2 改名+UI メニュー並び替え / **第2弾フェーズ0(整地)・1(Navigation)・2(Device)・3(Basic/Main/Data/Network)・4(View)・6(メニュー空セル統一)+任意項目全8件+タスク1(UI 視覚設計4画面)+タスク2(2-0 Profile 統合・2-1 EC・2-2 Kit/配信ほか)+タスク3(FontSize 全件=B群 Scene 25箇所+A群 41箇所)+UICockpit 廃止(UIメニュー10行化)+画面改名(Timeline/Flight/Tactical)**
- **残**: 画像アセットのコード反映(素材待ち)のみ → `UI_Verification_Checklist.md` 末尾。**スタイル切り出しは全モジュール完了(2026-07-08・36画面。詳細は下記「完了記録」)/ アイコンサイズの個別判断も全完了(Login 30→28)**
- **全変更コミット済み**(2026-07-12 時点。ビルドは各バッチで 0 エラー・新規警告ゼロ=既知の CS8785×1/XA4301×7 のみ)。**画面別の変更内容・実機確認観点は `UI_Verification_Checklist.md`**(uibase タグ→現在の全 UI 作業を画面単位で網羅・2026-07-12 再構成)

## これまでの経緯(時系列)

- **2026-06-13〜07-03 第1弾ブラッシュアップ**(旧 `_UIBrushUpPlan.md`・全フェーズ完了・コミット済み): Sample/UI 中心の約30画面。フェーズ0=バグ修正8画面 → 1=小規模改善 → 2=中規模(Login/Money/Mail/Character/Gauge/Pdf/Map1/Media/Chat/Pos/Social) → 3=描画コントロール拡張(Meter/Load/Radar/TreeMap/Mixer/CvNet) → 4=Effect 画面(ViewEffect)+Calendar 接続 → 5=新規7画面(Timeline/Profile/Schedule/Chart/Super/Cockpit/Chat) → 付録A=共通パターン横展開。この過程で AnimationOption(FadeUp/Pop/EnterDelay/EnterTrigger/Pulse/Wave/Bounce/FadeIn/Flash/Highlight)・Focus 枠・CountUp・BadgeCountConverter・PressEffect 等の資産群が確立
- **2026-07-03 UISample 取り込み**(旧 `_UI_Sample_Import_Plan(.Phase2).md`): 別リポジトリ Work-Project-MauiUI から既存 `Modules/UI` へ統合(UISample モジュールは新設しない方針)。**Ph1**=Profile2/TimelineSample(競合別名)+Feel/Pet/Stream×2/Shop×3/Kit×4 の14画面。**Ph2**=SkiaSharp 系4画面(FlightHud/MechHud/Energy/Telemetry)+コントロール5クラス(AnimatedSkiaView 基底+各 Screen)を Controls へフラット統合。SKPath 直接構築が CS0618 のため SKPathBuilder+Detach へ移行が必要だった
- **2026-07-04 アイコン共通化**: `Markup/FontIconExtensions.cs` の階層マークアップ拡張(基底 Material/Fluent+用途別 MenuIcon 48/MoneyIcon 9)へ72箇所置換。FontImageSource は Style 不可・マークアップ拡張はバインド不可(検証済)のため動的色の11箇所は FontImageSource 残置
- **2026-07-04 カスタム描画基盤の分離**: Graphics 配下を **Graphics.Drawing**(IDrawable/データ駆動: DrawingObject/DrawingControl/*Drawing)と **Graphics.Scene**(SKCanvas/自走: SceneObject/SceneControl/*Scene、旧 AnimatedSkiaView 後継)へ再編
- **2026-07-04 画面マージ検討**(旧 `UI_Screen_Merge_Analysis.md`): Timeline→**Graph2 改名実施**・**UI メニュー並び替え実施**(同種隣接+Row6 重複配置バグ解消)。方針決定=HUD4画面は独立維持/Profile は Profile2 ベースで統合(**2026-07-07 完了**=完了記録参照)
- **2026-07-06〜07 第2弾ブラッシュアップ**: 下の完了記録参照

## ユーザー決定の経緯(2026-07-07)

- **メニュー画面の空・無効ボタン整理は対応不要**。ただし「**空セルの扱いのみ『可視の無効ボタン』で統一**」を実施(下表)。アイコン化・入場アニメ等それ以外のメニュー統一は不要
- **旧 D1〜D6 の決定事項はすべてスコープ外(別途対応)**:
  - D1(NetworkHttp/Grpc/Scp)= 対応不要で確定(空実装・到達不能のまま現状維持)
  - D2(DeviceWiFi/Biometric)= 「Not implemented」空状態パネルを暫定実施済み。最終判断(機能実装/削除)は別途
  - D3(カレンダー採用 CalendarView⇔CalendarView2、現用は 2)/ D4(Profile 統合後の名前)/ D5(画像アセットの作成手段)/ D6(Radar/HUD 重複整理)= 別途対応
- **対応不要で削除した項目**: BasicLocale カルチャ切替 / SelectButton 左右角丸(`Button.CornerRadius` は全周一括のため Border ラップが必要になる)/ ViewCollection・ViewRefresh の行入場アニメ(リサイクル制約もあり)/ 共通部品化の追加検討 / 近縁ペアの扱い(TimelineSample↔Schedule 等)/ **遷移先の無い未結線ボタンへのトースト等メッセージ表示(未結線のままが仕様。UIShop 検索の実フィルタのみ実装)**/ **FontSize 修正での CockpitControls.cs**
- **FontSize の追加ルール(2026-07-07)**: Scene(Skia 自走描画)は情報量の観点から**ルール外の小フォントも許可**(ただし Excel 標準の 6/8/9/10 等を優先)。全体として **Android では WPF 版より小さく見えるため大きくする方向**で丸める
- **ドキュメント運用**: 当初はプラン=残項目のみ+記録=本書に分離(2026-07-07。旧プラン群 FontSize_Audit/UI_Dummy_Button_Plan/Image_Asset_Expansion_Plan/UI_Screen_Merge_Analysis/_系 を `UI_Implementation_Plan.md` と本書へ統合し削除)。**その後 2026-07-12 に `Style_Refactoring_Audit.md` と `UI_Implementation_Plan.md` も統合・削除し、最終的に本書 + `UI_Verification_Checklist.md`(uibase→現在の画面別変更チェックリスト)の2本に集約**

## 開発ポリシー(恒常・実装時は常に遵守)

- 共有 `Styles.xaml` は変更しない(**BasedOn 派生 or 新規リソース辞書**で対応)
- **View の code-behind 不使用**(Behavior / Trigger / VM / コントローラパターンで実装。再利用コントロールは `Controls/` に配置可)
- ビルド**警告ゼロ**(抑制が必要な場合は事前確認。Random の CA5394 のみファイル先頭 pragma の前例=UIRadarViewModel)
- フォントサイズは許可値のみ: `6, 8, 9, 10, 11, 12, 14, 16, 18, 20, 22, 24, 26, 28, 32, 36, 48, 72, 96, 160`
- アイコンは `markup:Material` / `markup:Fluent`(生 Unicode・絵文字は使わない)。サイズは Material スケール(18/24/36/48)推奨
- メニューは空セル=「可視の無効ボタン」で統一済み。それ以外のメニュー差異は**意図的なので統一しない**(下表)
- コミットは実機確認後にユーザーが実施

## メニュー画面の差異一覧(意図的差異の記録・2026-07-07 実態)

メニュー間の差異には**敢えて統一していない面がある**(ユーザー指示)。統一済みは「空セルの扱い」のみ。

| メニュー | ボタンスタイル | アイコン・絵文字 | 段数(行×列) | 空セルの扱い | ガード・その他 |
|---|---|---|---|---|---|
| Main/Menu(ルート) | `MenuButton`×9 | なし(「1.Basic」等の**番号プレフィックス**) | 9×1+フッタ | なし(全セル使用) | フッタに Flavor/Version 表示 |
| BasicMenu | `MenuButton`×9 | なし | 9×1 | 空の無効ボタン1(可視) | — |
| DeviceMenu | `MenuButton`×18 | なし | 9×2 | 無効ボタン2(WiFi/Biometric=**名前付き**・可視) | 遷移先は「Not implemented」パネル |
| NavigationMenu | `MenuButton`×11 | **絵文字プレフィックス**(🍇 Edit 等) | 9×2 | 空の無効ボタン3(可視) | — |
| NetworkMenu | `MenuButton`×12 | なし | 9×2 | 空の無効ボタン2(可視) | BaseAddress 未設定時は機能ボタン無効 |
| SampleMenu | `MenuIconButton`×11 | **Material**(`markup:MenuIcon`) | 9×2 | 空の無効ボタン7(可視)※旧・非表示→統一で可視化 | — |
| SampleCvNetMenu | `MenuIconButton`×5 | **Material**(`markup:MenuIcon`) | 9×1 | 空の無効ボタン4(可視)※旧・非表示→統一で可視化 | AI 未設定時はダイアログ |
| ViewMenu | `MenuButton`×18 | なし | 9×2 | 空の無効ボタン6(可視・灰色タイル) | — |
| UIMenu | `MenuIconButton`×30 | **Material**(`markup:MenuIcon`) | 10×3 | 空セルなし(全セル使用)※Profile2/Cockpit 廃止で 11 行→10 行に縮小 | XAML コメントで比較グルーピング |

- 残る差異(意図的に維持): ①アイコン=Material 3画面・絵文字 1画面・テキストのみ 5画面 ②番号プレフィックス=Main のみ ③列数=1/2/3列混在 ④入場アニメ・PressEffect は全メニュー未使用 ⑤ガード方式(Network=ボタン無効/CvNet=ダイアログ)

## 画面統合・類似性分析の結論(旧 UI_Screen_Merge_Analysis 要点)

- **画面そのものの統合価値が高いのは3組のみ**: ①FlightHud/MechHud/Telemetry/Energy(完全同型)→ **独立維持で確定** ②Profile/Profile2 → **Profile2 ベースで UIProfile へ統合完了(2026-07-07・経緯は完了記録参照)** ③Timeline/Graph(同一 Git グラフの異表現)→ **Graph2 改名で両立(実施済み)**
- その他(EC 3画面・Stream 親子・Chat/Mail・UIKit 5画面・Meter/Radar/Social 等)は**画面マージ非推奨**。部品/スタイル共通化の候補は挙がったが**対応不要で確定**(2026-07-07)
- **Radar/HUD 技術メモ**(D6=別途対応の材料): レーダー描画が2実装ある(`RadarScreen`=MAUI Graphics+外部バインド / `FlightHudScreen` 内蔵=SkiaSharp 自走)。整理するなら SkiaSharp 側(Scene 化)へ寄せるのが自然。両 API を跨ぐ描画共有は不可
- メニュー非掲載の7画面(UIItem/UICart/UIStreamDetail/UIKitNotify/UIKitSetting/UIKitOnboard/UIKitTracking)は**親子フロー**であり統合対象ではない

## 完了記録(第2弾)

- 2026-07-12 **ドキュメント再構成: 2本に集約** — `UI_Verification_Checklist.md` を **git `uibase` タグ(2026-05-16・UI 着手前)→ 現在の全変更を画面単位で網羅**する形に再構成(第1弾+UISample+第2弾+改名+スタイル切り出しを含む)。git 差分(名前状態+行数)で変更画面を機械的に洗い出し、Log の完了記録・旧 Checklist の確認項目と突き合わせて漏れなく反映。**変更記述は1行から箇条書き(変更要素の列挙)へ拡張**。`UI_Implementation_Plan.md` の残作業(画像アセットのコード反映)を Checklist 末尾へ移動し、**Plan は削除**。これで UI ドキュメントは **本書 + Checklist の2本のみ**。全変更コミット済み(未コミット0・未追跡0 を確認)
- 2026-07-12 **アイコンサイズの個別判断=全完了** — `UILoginView.xaml` のユーザー名/パスワード入力欄の左アイコン(`Account_circle`/`Key`)を **Size=30→28** に変更(ユーザー確定)。これで非標準アイコンサイズ 30 は残ゼロ、アイコン既定は **MenuIcon 24 / MoneyIcon 28 / Login 28**(全て標準値)で確定。プランの「アイコンサイズ(個別判断待ち)」章は完了につき削除。**プラン残項目はタスク4(画像アセットのコード反映・素材待ち)のみ**
- 2026-07-08 **スタイル切り出しの定義順統一(レビュー対応)** — ローカル ResourceDictionary の Style 定義順を「コンバータ→ルート要素(Root*)→PageStack→本文の出現順(コンテナ→中身、BasedOn 派生は基底の直後)」に全画面で統一。既存辞書へ追記した17画面(EditList/Status/Communication/Misc/BleScan/Behavior/Validation/Style/Locale/Setting/Data/Realtime/Svg/Animation/Graphics/Lottie/Refresh)を並べ替え(Nfc は既に適正・全面書き換え済みの画面は元から準拠)。並べ替えのみで定義内容は不変更。BasedOn のローカル前方参照なしを機械チェック済み。ビルド0エラー・新規警告ゼロ
- 2026-07-08 **スタイル切り出し: 全モジュール完了(計36画面)** — 最終バッチ=Main/SettingView+Data/DataView+Network/NetworkRealtimeView+View 6画面(Drawing/Refresh/Svg/Animation/Graphics/Lottie)。ViewDrawing はパレットチップを**色別派生 Style(BlackChip〜PurpleChip、選択 DataTrigger を Style.Triggers に内包)**にして本文の繰り返し6ブロックを解消。ViewAnimation のタイルも色別派生(Orange/Purple/PinkTile)。ViewRefresh はスケルトン Grid/細バー(SkeletonBarThin=BasedOn)とヘッダ行を Style 化。**残す直指定の最終形**=動作パラメータ(FocusedStroke/HighlightColor/ProgressColor/AccentColor/GraphColor/アニメ定義の Opacity・DataTrigger 状態色・Margin=-1・ColumnSpacing)+**FontImageSource(Style 不可の既知制約)**のみ。見た目の値は全画面不変更(純リファクタ)。ビルド0エラー・新規警告ゼロ
  - **対象=36画面**(第2弾で追加・改修した Navigation/Device/Basic/Main/Data/Network/View 系。UI モジュールは第1弾で整備済みのため元から準拠)。計測基準=`ContentView.Resources` の外(画面本文)に直書きされた視覚属性(FontSize/TextColor/FontAttributes/FontFamily/BackgroundColor/CornerRadius/Stroke系/Padding/Spacing/Width・HeightRequest/Opacity/TextAlignment)の数。`{Binding}` 動的値と Grid.Row 等の配置指定は対象外。「同じ見た目の定型が Style 化されず繰り返されている」ことを問題視した
  - **閾値以下で対象外(直書き数が僅少)**=Navigation 4(WizardInput1/2・SharedInput・EditDetail)/Device 4(QrScan・Camera・Info・Activity)/Basic 2(Converter・Dialog)
  - **意図的インラインで対応不要(C群=今後も触らない判断)**=BasicFont(フォント見本画面。FontFamily/FontSize のバリエーション自体がコンテンツ)/ViewEffect(演出資産カタログ。1点物のデモ要素中心)/DeviceSensor(軸バッジの RGB=XYZ 色などデータ対応バリエーション)/UIFeel(ムード別の色を per-cell 指定するのが仕様)/UIMeter(ABXY の AbsoluteLayout 配置座標)/メニュー系(UIMenu/ViewMenu 等=共有 MenuButton/MenuIconButton のみで構成=既に準拠)
  - **命名規約(全画面で統一)**=定型スタイルキー `RootGrid`/`RootScroll`/`PageStack`/`CenterCardBorder`/`CardStack`/`IconCircleBorder`/`IconCircleLabel`/`CenterTitleLabel`/`CardDivider`/`CenterHintLabel`/`EmptyStack`/`EmptyIconBorder`/`EmptyIconLabel`/`EmptyTitleLabel`/`EmptyHintLabel` 等。色違いの繰り返しは BasedOn 派生(ViewDrawing の色チップ・ViewAnimation のタイル・Communication の Dial/Sms/Email 等)。定義順=コンバータ→ルート(Root*)→PageStack→本文の出現順(コンテナ→中身、BasedOn 派生は基底の直後)
- 2026-07-08 **スタイル切り出し: Device(13画面)+Basic(5画面)完了** — Device=QrDisplay/BleHost/Ocr/Bluetooth/WiFi/Biometric(A群)+Location/Audio/BleScan/Nfc/Status/Communication/Misc(B群)、Basic=Typography/Locale/Behavior/Validation/Style。Navigation と同じ定型キー(RootGrid・RootScroll/PageStack/CenterCardBorder/CardStack/IconCircle*/CenterTitleLabel/CardDivider/CenterHintLabel/Empty* 系)+画面固有キー(QrFrame/Transport/Volume/Battery/Network/Listening 等)。Communication のアイコン円色違いは Dial/Sms/Email 派生 Style(BasedOn ActionIcon*)、Bluetooth の Print は PrimaryButton 派生。**対応後も残す直指定=動作パラメータ**(Focus.FocusedStroke/HighlightColor/ProgressColor/StepIndicator.AccentColor、DataTrigger 状態色、セグメント重ね Margin=-1、行 Grid の ColumnSpacing)の計13箇所のみ=スタイル化対象外と整理。閾値以下の非対象=Device 4画面(QrScan/Camera/Info/Activity)・Basic 2画面(Converter/Dialog)。見た目の値は不変更(純リファクタ)。ビルド0エラー・新規警告ゼロ。**残=Main(Setting)/Data/Network→View**
- 2026-07-08 **スタイル切り出し: Navigation モジュール完了(9画面)** — A/B 群のうち Navigation 分を画面ローカル ResourceDictionary へ切り出し。対象=Stack1〜3/SharedMain1〜2/NavigateInitialize/NavigateCancel(A群)+WizardResult/EditList(B群)。**中央カード定型のスタイルキーを画面間で統一**: `RootGrid`/`CenterCardBorder`/`CardStack`/`IconCircleBorder`/`IconCircleLabel`(SharedMain は `NumberCircle*`)/`CenterTitleLabel`(NameLabel 派生)/`CardDivider`/`CenterHintLabel`(ValueLabel 派生)。StatusChip は `TargetType="controls:StatusChip"` の Style で色設定可(SharedInputChip)。計測でインライン視覚属性は全画面 0 に(残る直指定は StepIndicator.AccentColor=コントロールへの系統色パラメータと行 Grid の ColumnSpacing のみ)。WizardInput1/2・SharedInput・EditDetail(各5件・FieldBorder 適用済み)は閾値以下で対象外。**ビルド時に NU1605(Usa.Smart.Core 2.13.0→2.12.0 ダウングレード検出)が発生**=MauiComponents 側の更新への追随として csproj の `Usa.Smart.Core` を 2.12.0→2.13.0 に更新して解消。ビルド0エラー・新規警告ゼロ
- 2026-07-08 **画面改名(3件)+小修正(2件)** — ①**UITimelineSample→UITimeline**(表示名 Timeline)/**UIFlightHud→UIFlight**(Flight)/**UIMechHud→UITactical**(Tactical)。ViewId/View/VM/ファイル名/メニューボタン/タイトルを一括変更([ViewSource] 命名規約で登録は自動追従)。Scene クラス(FlightHudScene/MechHudScene)は Graph2 改名時の前例に合わせ内部型として据え置き。FontSize_FlightHud/MechHud_*.png のファイル名も撮影当時のまま据え置き。※第1弾で stash 退避中の「UITimeline stub (WIP)」とは名前衝突するため、pop する場合は要注意 ②**UICalendar のタップ通知をトースト化**: Day/Event タップの `Debug.WriteLine` を `IDialog.Toast` に変更(日付=yyyy/MM/dd、イベント=タイトル。LoadMonth の計測ログは残置)。コマンドは MakeAsyncCommand 化 ③**UIItem の Add to Cart から `BounceTrigger`(Quantity 連動)を削除**: 数量ボタン操作で別ボタンが弾む演出は不要とのレビュー(Bounce 中にボタン角丸が崩れて見える事象も同時解消)。ビルド0エラー・新規警告ゼロ
- 2026-07-07 **タスク3 A群(FontSize XAML/C# 41箇所)完了 — これで FontSize 統一は全完了** — 値検索で全ファイルを走査し**大きい側へ置換**: 13→14(29箇所: Cart明細/Chatバブル/Graph・Graph2/Item/KitDash/KitTracking/Profile職業・Bio/Shop/StreamDetail/Super/TimelineSample/ViewEffect×5/SampleChart)、15→16(7箇所: Cart合計・Checkout/Feel/Item/Pet/Shop検索/TimelineSampleタイトル)、17→18(1箇所: Schedule)、C# 13d→14d(CalendarView/CalendarView2 の DateNumber/WeekdayHeader/Year 既定値6箇所。SlotRowHeight 17d/EventRowHeight 15d は行高のため対象外)。監査44件との差=Profile2/Cockpit 廃止分の消滅+ViewEffect の監査外1件を追加検出。レイアウトは固定幅トラップなしと静的確認のうえ、実機で Calendar(日付14pt)/Profile(職業行1行維持・Bio折返し)/UIメニューをスポット確認。ビルド0エラー・新規警告ゼロ
- 2026-07-07 **メニューアイコン 24→28**(ユーザー指示「メニューボタン系はまず28で確認」) — `MenuIconExtension` 既定を 24d→28d(UIメニュー/Sample/SampleCvNet の全メニューボタンに波及)。実機スポットでは崩れなし(ボタン文字のディセンダーがやや詰まって見える可能性あり=要ユーザー確認)。**残る非標準アイコン30は MoneyIcon 既定 30d と UILoginView の30×2 のみ=個別判断待ち**(他の Size 指定は全て標準値 18/24/28/36 だった)
- 2026-07-07 **UICockpit 廃止**(ユーザー決定) — FlightHud との比較で「Cockpit 固有はクラシック水平儀の表現(空/大地の2色ホライズン盤+ウイング機体マーク)のみ、情報は FlightHud で全てカバー、下部 Gauge×3 は不要」と評価し廃止を決定。削除=`UICockpitView.xaml(+.cs)`/`UICockpitViewModel.cs`/`Controls/CockpitControls.cs`(AttitudeIndicator/CompassRibbon。利用箇所が UICockpit のみのため同時削除)/`ViewId.UICockpit`/UIメニューの Cockpit ボタン。**UIメニューは1項目減で 11行→10行に縮小**(後続6ボタン繰り上げ、30ボタンで全セルが埋まり空セル=無効ボタンは0個に)。`Controls/Gauge` 本体は UIGauge 等で使用継続のため無影響。残参照ゼロ確認・ビルド0エラー・新規警告ゼロ
- 2026-07-07 **タスク3 B群(Skia/Scene 描画テキスト 26件)完了** — **CockpitControls.cs はユーザー指示で対応不要**とし、Scene 4ファイル25箇所を修正。**方針(ユーザー指示)**: ①Scene は情報量の観点からルール外の小フォントも許可されるが、Excel 標準値(6/8/9/10 等)を優先 ②Android 実機は WPF 版より小さく見えるため**大きくする方向**で丸める。適用: **8.5→9 / 9.5→10 / 7.5→8 / 7→8 / 13→14 / 15→16 / 17→18**、Telemetry の GEAR 桁のみ **56→72**(84×104 パネル内に収まる計算を確認)。レイアウト定数の変更は不要(全て既存間隔で吸収、タープ目盛はガード条件 |y−cy|>14 が 9pt でも干渉しないことを計算確認)。**比較用スナップショットを Document/FontSize_{FlightHud|MechHud|Telemetry|Energy}_{Before|After}.png に保存**(実機 Pixel・adbで撮影)。ビルド0エラー・新規警告ゼロ・4画面とも実機表示確認済み(崩れなし)
- 2026-07-07 **タスク2 優先度C分=「メッセージ表示は不採用・未結線のまま」で確定** — 一度 `IDialog.Toast` による8画面一括結線(Money メニューアイコン/Mail 通知・検索・FAB/Social タイル/Super 検索・ミニアプリ/Stream 検索・通知・See all/StreamDetail Play・Share/Login パスワードリンク/Shop Filter)を実装したが、**「未実装のボタンに対してメッセージを表示する対応は不要」とのユーザー決定で全戻し**。該当ボタンは未結線のままが仕様。**UIShop の検索 Entry のみ実機能として存続**(`SearchText`+NotifyAlso 計算プロパティで All Items をタイトル部分一致絞り込み。Entry の Text バインドも維持)。※技術メモ: トースト基盤自体は既存 `IDialog.Toast` で足りる(BasicDialog/MainPage に前例)、SfEffectsView は `TouchDownCommand`+`TouchDownCommandParameter` を持つ(Social 作戦中止の Back 結線で使用中)
- 2026-07-07 **タスク2-0(Profile 統合)完了** — **段階実施方式**(最初の一括統合はユーザー指示で全戻しし、チェックリストmdで1項目ずつ確認しながら実施(役目を終えたファイルは削除済み))。①**名称入れ替え**: Profile=旧 Profile2 の内容(カード型。モデルを `UIProfileStat/Interest/Photo` に改名)/Profile2=旧 Profile の内容。ファイル相互書き換え方式(code-behind 無変更、[ViewSource] の命名規約で ViewId↔View の対応が自動で入れ替わる)②**取り込みの決定**: 基本要素は旧 Profile2 の内容を採用(写真6枚グリッド/色付きタグ/区切り線統計/Bio/画像/統計値=全て現状維持)、名前のみ「山奥 うさぎ, 29」へ変更、**旧 Profile 優位の UI 要素=パララックスヘッダーのみ移植**(`Scroll.ParallaxTarget`+カバー Grid ラップ+不透明ブロックで透け防止)③**演出・結線は FadeUp 以外を適用**(ユーザー指示で入場アニメ不採用): **SNSアクション3トグル**=フォロー(Person_add→How_to_reg・青反転。「×=パス」はSNS的に意味不明とのレビューでフォローに置換)/いいね(Favorite_outline→Favorite・ピンク反転+`BounceTrigger`)/お気に入り(Star_outline→Star・琥珀反転)、全ボタン PressEffect、♡★は初期ON・フォローは初期OFF(追加指示)。トグルは3つとも「OFF=白地+色アイコン/ON=色地+白アイコン」で統一 ④**UIProfile2 削除**: View/VM 3ファイル・`ViewId.UIProfile2`・メニューボタン削除+後続22ボタン繰り上げ(末尾無効ボタン2個)。残参照ゼロ確認。ビルド0エラー・新規警告ゼロ
- 2026-07-07 **タスク1(UI 視覚設計4画面)実装完了** — **UIFeel**: 7つの hex セルにタップ選択を実装(VM に `SelectedMood`+`SelectCommand`、各セル=TapGesture+DataTrigger で自系統色の枠3px+チェックバッジが選択セルへ移動、バッジは表示時 `Pop`)、ハニカム全体も中央→外周の `Pop` 開花入場(0/80/140/200/260ms)。旧 HexPathSelected 固定スタイルは廃止 / **UIKitTracking**: ステップモデルに `Current`/`Delay` を追加し3状態化(完了=緑+白チェックアイコン重ね、進行中=青ドット `Pulse`+タイトル青、未来=灰)、完了区間のタイムライン縦線を緑に塗り分け、行は `FadeUp` 段差(BindableLayout なのでリサイクル問題なし) / **UIKitNotify**: 項目を ObservableObject 化して `IsUnread` を追加(先頭3件未読)、未読=左アクセントバー4px+青背景+太字+未読ドット、既読=白背景・通常ウェイト、**行タップで既読化**(`ReadCommand`+AncestorType バインド。行入場は CollectionView リサイクルのため不採用) / **UIPet**: **`AnimationOption.ProgressTo` を新設**(ProgressBar を Loaded/値変更時に 800ms CubicOut で伸長させる添付プロパティ)し、ステータスバー4本を星比率 Grid→ProgressBar 化して 0→実値アニメ+数値 `CountUp`、シート `FadeUp`、**Heart 結線**(HP+5/上限400、バー・数値連動+`BounceTrigger`)+**Add to Party トグル**(✓ In Party+緑)。ビルド0エラー・新規警告ゼロ
- 2026-07-07 **タスク2-1(EC 3画面)実装完了** — **UICart**: `UICartItem` を ObservableObject 化(`UnitPrice` decimal+`Quantity` int+`Delay`)し**ステッパー結線+合計再計算**(`Recalculate()` で ItemCount/Subtotal/Discount10%/Total を一括更新、数量1..99)、明細行 `FadeUp` 段差、**Total は `CountUp`(450ms)+`BounceTrigger`**、金額は StringFormat で従来表記維持、**Checkout 結線**(IDialog 完了ダイアログ)。※dialog は ctor 引数のラムダ閉包で使用(フィールド保持だと IDE0052) / **UIItem**: VM に `Quantity`/`SelectedSize`+増減・サイズ選択コマンド、**サイズタグ 30/50/100ml タップ切替**(DataTrigger 活性化、旧 Active 固定スタイル廃止)、商品画像+皿 `Pop` 入場、Add to Cart に `BounceTrigger`(数量変更で弾む)+PressEffect / **UIShop**: 商品カード(Popular 横+All Items 2列)を **SfEffectsView Ripple** 化、ヘッダ→検索→見出し→リストの `FadeUp` 段差(カード個別入場はリサイクルのため container 単位)。検索/Filter 結線は優先度C見送り。ビルド0エラー・新規警告ゼロ
- 2026-07-07 **タスク2-2(Kit/配信/その他 7画面)実装完了** — **UIKitOnboard**: **Skip/Get Started 結線**(`CompleteCommand`→UIKitDash)+ページ切替でテキストが `FadeInTrigger`(CarouselView.CurrentItem を x:Reference バインド=ViewCarousel と同手法) / **UIKitSetting**: **Switch 実バインド化**(項目を ObservableObject 化し getter-only `IsOn`→[ObservableProperty])+グループ `FadeUp` 段差 / **UIKitDash**: メトリクス4カード `Pop` 段差+Heart Rate カード `FadeUp`+ベル未読ドット `Pulse`+リンク2カード Ripple+`FadeUp` / **UIStream**: **「+ My List」トグル結線**(`InMyList`+DataTrigger で「✓ In My List」+赤背景)、TopBar/Hero `FadeUp`+レーティング `Pop`、セクション棚はモデル `Delay` で `FadeUp` 段差(BindableLayout)、ポスター Ripple+各ボタン PressEffect / **UIStreamDetail**: **タブ切替で Trailers/Related が `FadeInTrigger`**、**Favorite トグル**(outline→filled 赤+`BounceTrigger`)+**Download トグル**(→緑チェック)、Trailer 行・Related タイル Ripple(Play/Resume/Share 結線は優先度C見送り) / **UITimelineSample**: 進行中ドットの halo に `Pulse="{Binding Current}"`(バインド式=リサイクル安全)、ヘッダ+リスト container `FadeUp` / **UIGraph2**: リスト container `FadeUp`。ビルド0エラー・新規警告ゼロ
- 2026-07-07 **フェーズ1(Navigation 底上げ)実装完了** — **共通部品**: `Controls/StepIndicator` 新設(C# 構築の ContentView。CurrentStep/TotalSteps/AccentColor、●を横に並べ現在位置をピル型強調+「n / N」ラベル。Wizard と Stack で兼用)。入力フィールド定型(FieldCaptionLabel+FieldBorder+`Focus.FocusedStroke`)とサマリカード(既存 `InfoCard` 流用)を確立。**Wizard 3画面**: `WizardContext` を **ObservableObject 化**して **Next(▶️)の入力ゲート**を実現(`Function4Enabled` に NotEmptyConverter)、StepIndicator(1〜3/3)+Step カード+ヒント、Result はサマリカード+Done カード+Function4Text を ✔️ に / **Stack 3画面**: 中央カード+レベル別アクセント(1=Blue/2=Teal/3=DeepPurple)の Looks_one〜3 アイコン円+StepIndicator(同色)+キー操作ヒント+`Pop` 入場 / **Shared 3画面**: SharedInput は VM の nextViewId を **NextViewId/NextName として公開**し「Return to Shared1/2」チップ(系統色 Indigo/Teal)+入力フィールド定型、Main1/Main2 は系統色の数字円+チップ+No 大型表示+FadeUp / **EditList**: 行を白カード化(Id=等幅 #n ピル+Edit/Delete に PressEffect)+EmptyView(Inbox+「Press New」)/ **EditDetail**: 入力フィールド定型+インラインエラー / **NavigateCancel**: パターン説明カード常設(Amber Help+Yes/No の挙動説明)/ **NavigateInitialize**: VM に `Initialized` を追加、初期化中は Hourglass Pulse+スケルトン → 完了で緑 Task_alt カードが FadeIn / **InputNumber**: ローカル派生スタイル(Pad*Button=共有 Input*Button+PressEffect)で全キーに押下フィードバック、✔/❌ を Material Check/Close に置換。ビルド0エラー・新規警告ゼロ
- 2026-07-07 **任意項目 全8件 実装完了** — **BleScan**: `CollectionView.EmptyView`(Bluetooth_searching アイコン Pulse+「Scanning...」)+温湿度/CO2 値の更新時 `BounceTrigger` / **Nfc**: 履歴 EmptyView に「Suica をかざしてください」(Contactless アイコン Pulse・ダークテーマ)+残高を `LabelOption.CountUp` 化(600ms) / **QrScan**: 結果空時の「Scan a code...」プレースホルダ+検出時にカメラ面へ白 `FlashTrigger` / **Activity**: 歩数(96pt)を CountUp 化 / **Sensor**: `Graphics/Drawing/SensorDrawing.cs` 新設=**CompassDrawing**(30°目盛ダイヤル+N/E/S/W+磁北を指す赤/灰の針、DrawingControl 120px で回転アイコンを置換)と **LevelDrawing**(気泡水準器=加速度 X/Y で気泡移動・水平時に緑。新規 Level カード追加、入場遅延を 320/400/480 に再配分)、VM の購読で Heading/Gravity を更新し Invalidate / **Location**: `MapController.MoveTo(lat, lon, radius)` を追加し、画面上部に 240px の `maps:Map`(`IsShowingUser`)を常設、初期は東京駅→測位毎に現在地へ移動(※タイル表示は Google Maps API キー設定後) / **ViewEasing**: `Controls/EasingCurveView` 新設(GraphicsView+IDrawable、Easing 曲線をポリラインで背景描画、Spring/Bounce のオーバーシュート用に上下 18% 余白)を全11タイルに敷設 / **ViewRefresh**: 初回ロード(1秒)中に `CollectionView.EmptyView` のスケルトン4行(丸+バー2本のカード)。CA2000(PathF)は `using var` で解消。ビルド0エラー・新規警告ゼロ
- 2026-07-07 **フェーズ6(メニュー統一=空セルのみ)完了** — **空セルの扱いを「可視の無効ボタン」に全メニュー統一**: SampleMenu(7箇所)/SampleCvNetMenu(4箇所)の `IsVisible="False"` を除去して無効ボタンを可視化、UIMenu の空きセル(Row10/Col2)に無効の `MenuIconButton` を追加。アイコン化・入場アニメ等それ以外の統一は不要(ユーザー決定)。ビルド0エラー・新規警告ゼロ
- 2026-07-07 **フェーズ0(整地)完了** — Wizard: `WizardInput2View.xaml` の Placeholder "Data1"→"Data2" / NetworkMenu: VM の未使用 `RealtimeCommand`(デッドコード)削除 / ViewBorder: コメントアウトされていた StrokeLineJoin・StrokeLineCap の Picker を復活(VM 側は実装済みだった)/ UIKitSetting: chevron の生 Unicode `&#xe5cc;` を `x:Static fonts:MaterialIcons.Chevron_right` へ統一。DeviceStatus 開発パレット削除・BasicFont/Typography の ScrollView 化・BasicConverter のタイポ+ToChecked 削除はフェーズ2・3 実施時に対応済み。メニュー空ボタン整理と NetworkHttp/Grpc/Scp 削除はユーザー決定により対象外。ビルド0エラー・新規警告ゼロ
- 2026-07-07 **ViewCarousel 中央カード強調 追加実装** — `PhotoItem` を ObservableObject 化して `IsCurrent` を追加、VM の `CurrentChangedCommand`(`CarouselView.CurrentItemChangedCommand`+CurrentItem パラメータ)で現在項目をマークし、DataTrigger で**非中央カードを Scale 0.92+Opacity 0.55 に減衰**(スナップ確定時に切替)。ビルド0エラー・新規警告ゼロ
- 2026-07-07 **フェーズ4(View 磨き)実装完了** — Graphics(**インタラクティブ化**: `ShapeDrawing` に `Circle` 図形を追加し、Add line/circle/rect(乱数色=HSL)/Clear ボタン+図形数チップで Invalidate 再描画を体感できる構成へ)/ Drawing(**色6チップ+線幅 Slider のパレット**=DrawingView の LineColor/LineWidth に VM バインド、選択チップは DataTrigger で枠強調、プレビュー額装+HasImage 空状態)/ Easing(**4×3 均等グリッドへ再設計**=白カード+灰トラック+ボール、12セル目は実行状態セル=Run 中 Pulse+Function4 を実行中無効化)/ Lottie(**プレイヤー UI**=シーク Slider+mm:ss.f 等幅表示+円形 Play/Pause・Reset、白カード額装)/ Svg(**3 SVG 切替**=dotnet_bot/vite/react(web-app 資産を流用)チップボタン+ファイル名表示+額装)/ Effect(**資産カタログ完成**: 常時アニメ=Wave×3 時間差+Pulse+ON/OFF Switch、変化フィードバック=Bounce+Highlight+Flash シャッター発光を Fire ボタンで同時発火)/ Animation(2 InfoCard 化=Click 系4ボタン(**Shake 新設**)+Tap 系タイル、空セル解消)/ Collection(▼▲→`Expand_less/more` アイコン、スワイプ項目をアイコン+文字の縦積みに)/ Refresh(ヘッダに Newspaper アイコン+**件数ピル**、行カード白+枠線化+リスト背景グレー)/ Carousel(カード白+影、画像角丸クリップ、背景グレー)。ビルド0エラー・新規警告ゼロ
- 2026-07-06 **フェーズ3(Basic/Main/Data/Network 底上げ)実装完了** — Behavior(2 Entry を Border+`Focus.FocusedStroke` 青/緑で色分け+イベント値の Highlight カード)/ Validation(**ルートの `ContentPage.Behaviors` 誤記を `ContentView.Behaviors` に修正**、エラーで枠 Border が赤 Highlight+エラーラベル FadeIn、Error/Clear/Focus をアイコン付き ActionButton 化)/ Converter(**タイポ Upper/Lowwer 修正+未使用 ToChecked 削除**、CheckBox+ラベル横並び化、変換結果に Highlight)/ Style(全 Action/Information ボタンを **`ActionCommand` で結線し「Last action」カードに Highlight 表示**、SelectButton は隣接セグメント配置)/ Font(**ScrollView 化**、フォント毎の InfoCard 10枚+**JetBrainsMono/NotoSerifJP 見本追加**=NotoSerifJP は `MauiProgram.ConfigureFonts` に登録追加)/ Typography(ScrollView 化+4 セクションカード)/ Locale(現在カルチャカード+リソースキー2件の一覧)/ Dialog(11 ボタンを4カテゴリ InfoCard に整理)/ Data(CRUD/Bulk 2カード+**BulkDataCount に CountUp**)/ NetworkRealtime(**TODO 解消: 鋸波→ランダムウォーク擬似データ**=CPU ゆらぎ+スパイク・Memory ドリフト・Network バースト、**StatControl 3枚**=青/緑/橙で色はリソース参照化、500ms 更新。CA5394 は UIRadarViewModel と同じファイル先頭 pragma 方式)/ Setting(スキャンガイドピル+設定値パネル、値変更時 Highlight、未設定時「(not set)」)。ビルド0エラー・新規警告ゼロ
- 2026-07-06 **フェーズ2(Device 底上げ)実装完了** — **共通部品**: `Controls/InfoCard`(見出しアイコン+タイトル+区切り線付き角丸カード、ControlTemplate+ContentPresenter 方式)/ `Controls/StatusChip`(アイコン+テキストのピル型チップ、DataTrigger で状態色分け)/ `Converters/CenteredRatioConverter`(±Range→0〜1 正規化)を新設。**画面**: Info(3 InfoCard 化+FadeUp 段差)/ Sensor(**Vector3/Quaternion を VM で軸分解**=NotifyAlso 連動の計算プロパティ、RGB=XYZ 軸バッジ+中央ティック付き ProgressBar、Compass+Barometer)/ Status(**開発用バッテリーアイコン列挙を削除**、動的バッテリーアイコン+残量ゲージ=20%以下赤+充電中緑、Network 状態チップ=緑/琥珀/赤)/ Audio(**プレイヤーカード UI**=250ms ポーリング、シーク Slider+`SliderOption.DragCompletedCommand`、円形トランスポート3ボタン、再生中 Pulse、音量%は x:Reference)/ QrDisplay(**Entry ライブ編集→QR 再生成**+額装+空状態)/ Location(**測位待ち空状態**+Position/Motion カード+タイムスタンプ Highlight)/ Ocr(`CameraOverlayView` ガイド枠+**画面内結果パネル**=ダイアログ廃止+実行中オーバーレイ)/ Bluetooth(**State enum のインライン状態チップ**=Idle/Connecting/Printing/Completed/Failed+ダイアログ廃止)/ Misc(**4 InfoCard 分類**=Screen/Feedback/Light/Speech、15アクションに Material アイコン付き ActionButton、音声認識 IsListening=赤マイク Pulse)/ Communication(**タップ行カード×3**=色分けアイコン円+SfEffectsView Ripple)/ BleHost(Podcasts アイコン円=Advertising 中 Pulse+状態チップ)/ WiFi・Biometric に「Not implemented」空状態パネル。ビルド0エラー・新規警告ゼロ

## 調査メモ(2026-07-06 の5並列調査より)

- 全 Modules 約104画面。実装前の未改善は約60画面。**質感差の正体は「エントランス/フィードバック演出の有無」**(改善済みは AnimationOption 多用=UIMoney 14 箇所、未改善はほぼゼロ)
- 実装前の状態: Navigation=15画面中12が C(Stack 本文空・Wizard ステップ表示なし)/ Device=約9が C(Sensor 生 ToString・資産未使用)/ Basic 系=Font/Typography が実機クリップ / View=Menu 灰色タイル・Graphics 等素朴 / UI 未改善19画面=主要ボタン未結線
- **Device の A 水準画面**(手本): DeviceQrScan(検出オーバーレイ)、DeviceBleScan(カード+DSEG7)、DeviceNfc(Suica 風)、DeviceActivity(歩数ビジュアル)
- **既知の環境制約**: SampleMap1/DeviceLocation の地図タイルは Google Maps API キー未設定で非表示(Pin・カメラは動作)/ SampleCvNet 系は AI エンドポイント未設定で画面に入れない / CommunityToolkit CameraView の `CaptureAsync` がまれに未完了で Function キーが無反応化(再起動で回復。恒久対応するならタイムアウト付き待機)

## 技術ナレッジ(実装で確定)

- **正確な API 名**(発明注意): CountUp=`LabelOption.CountUpValue/CountUpFormat/CountUpDuration`、フォーカス枠=`Focus.FocusedStroke/FocusedThickness`(親 Border 必須)、Make* コマンドヘルパは canExecute 自動再評価(`.Observe()` は存在しない)、`s:NullToText` は Null/NonNull 置換のみ(値パススルー不可→空状態は2ラベル+`NullToBoolConverter`)
- **Vector3/Quaternion の X/Y/Z はフィールド**のためバインド不可 → VM で NotifyAlso 連動の計算プロパティに分解
- **MAUI `Button.CornerRadius` は全周一括** = 左右個別の角丸は不可(SelectButton の共有 Style TODO が残る理由)
- **PathF は IDisposable**(CA2000)→ `using var path = new PathF()`。CA5394(Random)は `UIRadarViewModel` 前例=ファイル先頭 `#pragma warning disable`。算術式は括弧明示(IDE0048/SA1407)
- **CollectionView 行の入場アニメは不成立**(リサイクルで Loaded 再発火。第1弾 UIChat と同判断)
- **SkiaSharp**: `SKPath` 直接構築は CS0618 → `SKPathBuilder`+`Detach()`。`DrawText` は SKTextAlign 付き、`DrawBitmap` は SKSamplingOptions 付きオーバーロードを使用。日本語は `SocialFonts.NotoSerifJP`(SKTypeface.Default は豆腐化)、絵文字は `SKFontManager.MatchCharacter`+異体字セレクタ U+FE0F 除去
- **FontImageSource は Style 不可・マークアップ拡張はバインド不可**(検証済)→ 動的色は FontImageSource 直書き
- ControlTemplate+ContentPresenter+`TemplateBinding` に Converter 指定可(InfoCard/StatusChip で実証)。CarouselView の中央強調は項目モデルの IsCurrent+`CurrentItemChangedCommand`+DataTrigger で code-behind 不要
- Device/Navigation 系の画面定型: **GrayLighten5 背景+Padding 12+InfoCard+FadeUp 段差(0/80/160…)**、単一機能画面は**中央カード+円形アイコン(96)+説明+ヒント**
- ビルド警告ベースライン=8件(CS8785×1=Smart 生成器、XA4301×7=ネイティブ lib 重複)。`dotnet build` 約2分(net10.0-android)。Visual Studio 併用時は obj のファイルロック(XARDF7024/XARLP7000)で CLI ビルドが失敗することがある=リトライか該当 VS を閉じる

### 資産レシピ表 — 既存資産の正確な名前(適用時のコピー元)

| 資産 | XAML での書き方 | 主な用途 |
|---|---|---|
| エントランス | `behaviors:AnimationOption.EnterAnimation="FadeUp\|Pop"` + `EnterDelay`(ms 段差) + `EnterTrigger`(再実行) | 静的カード/リスト行の入場 |
| 常時アニメ | `behaviors:AnimationOption.Pulse` / `Wave`+`WaveDelay` | 進行中ドット、待受/スキャン中の生感 |
| 変化フィードバック | `behaviors:AnimationOption.BounceTrigger`+`BounceValue` / `FlashTrigger` / `FadeInTrigger` / `HighlightTrigger`+`HighlightColor` | 値更新、追加/削除、タブ切替、CTA 押下 |
| バー伸長 | `behaviors:AnimationOption.ProgressTo`(ProgressBar を 800ms CubicOut で伸長) | ステータスバー・ゲージ |
| カウントアップ | `behaviors:LabelOption.CountUpValue`+`CountUpFormat`(+`CountUpDuration`) | 金額・件数・歩数(Loaded 数え上げ対応) |
| フォーカス枠 | `behaviors:Focus.FocusedStroke`+`FocusedThickness`(**親 Border 必須**) | Entry/Editor の入力体験 |
| 押下 | `behaviors:ButtonOption.PressEffect="True"`(Button/ImageButton)+`HapticFeedback` / `toolkit:SfEffectsView TouchDownEffects="Ripple"`(+`TouchDownCommand`/`TouchDownCommandParameter`) | 全タップ要素 |
| バッジ | `converters:BadgeCountConverter`(0→空、Max 超→「99+」) | 件数バッジ |
| アイコン | `{markup:Material Glyph={x:Static fonts:MaterialIcons.Xxx}, Color=.., Size=..}` / `{markup:Fluent ..}` / `{markup:MenuIcon ..}` | 絵文字・生 Unicode の置換(バインド不可な点に注意) |
| カード/チップ/ステップ | `controls:InfoCard`(Title/Icon/IconColor+Content)/ `controls:StatusChip`(Text/Icon/ChipColor/IconColor/TextColor)/ `controls:StepIndicator`(CurrentStep/TotalSteps/AccentColor) | 第2弾で新設した共通部品 |
| 空状態 | `CollectionView.EmptyView` / 中央 VStack+円形アイコン(96)+説明の定型 | 0件/未取得/未実装の表示 |
| その他 | `CameraOverlayView`(撮影ガイド枠)/ `MapBind`+`MapController(.MoveTo)` / `EasingCurveView` / `JetBrainsMono`(等幅数値)/ `NotoSerifJP`(Skia 日本語) | — |
