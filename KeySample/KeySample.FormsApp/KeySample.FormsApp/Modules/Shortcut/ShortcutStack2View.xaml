<?xml version="1.0" encoding="UTF-8" ?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:sr="clr-namespace:Smart.Forms.Resolver;assembly=Smart.Forms"
             xmlns:shell="clr-namespace:KeySample.FormsApp.Shell;assembly=KeySample.FormsApp"
             xmlns:modules="clr-namespace:KeySample.FormsApp.Modules.Shortcut;assembly=KeySample.FormsApp"
             x:Class="KeySample.FormsApp.Modules.Shortcut.ShortcutStack2View"
             x:DataType="{x:Type modules:ShortcutStack2ViewModel}"
             sr:BindingContextResolver.Type="{x:Type modules:ShortcutStack2ViewModel}"
             shell:ShellProperty.Title="Stack"
             shell:ShellProperty.Function1Text="Back"
             shell:ShellProperty.Function1Enabled="True">

    <Grid Style="{StaticResource FillGrid}">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

    </Grid>

</ContentView>
